# Session export (2026-08-21)

Saved so a Windows reinstall does not wipe the Grok chat or workshop context.

**Start here:** [HANDOFF.md](HANDOFF.md)

| File | Use |
| --- | --- |
| `HANDOFF.md` | How the plugin works, logins, bakery numbers, restore steps |
| `CHAT-RECENT.md` | Readable Q&A from this session (after compaction) |
| `local_campusupdates-plugin.zip` | Plugin snapshot — unzip into Moodle `local/campusupdates` |
| `moodle-db.sql` | MariaDB dump of the local demo site (admin / CampusDemo1!) |
| `raw/` | Machine copies of the Grok session (`chat_history.jsonl`, compaction, summary) |

The live plugin source is still `../../local/campusupdates/`. The PDF spec is in the repo root as `workshop-proposal.pdf`.
